import { makeStyles } from '@material-ui/core';

export const Style = makeStyles({
    contaoner: {
        padding: '0px 5rem'
    },
    districtOption: {
        height: '2.3rem',
        width: '11rem',
        fontWeight: '700',
        marginTop: '2rem'
    }
})